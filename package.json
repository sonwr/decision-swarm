{
  "name": "decision-swarm",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test ./tests/*.test.mjs",
    "test:update-snapshot": "UPDATE_SNAPSHOT=1 node --test ./tests/*.test.mjs"
  }
}
